# アウトライン描画のノートPC負荷検証 — Issue #45

> このドキュメントは**記録用の雛形**です。数値・所見は実機で計測した人が埋めてください。
> 検証の目的は「祭事時に最大15体＋α＝16体同時のアウトライン描画が、
> 展示用ノートPCで 60fps（暫定目標）を満たすか」を実測すること。

---

## 0. 検証環境

- シーン: `Assets/Members/Jungaya/Scenes/OutlinePerf.unity`
- 生成メニュー: `Toufuku/Perf/Build Outline Perf Scene`
- Unity: 6000.3.13f1 / URP 17.3.0（RenderGraph / Forward+）
- レンダラ: `Assets/Settings/PC_Renderer.asset`
  - Rendering Path: **Forward+**（ステンシル下位ビットが Deferred のマテリアルフラグと衝突するため）
  - Native RenderPass: **OFF**（フルスクリーン Compose の depth-stencil バインドと干渉しうるため）
- 実施日: ____年__月__日
- 実施者: ____________
- 実機: ____________（ノートPC名・GPU）

### 操作キー

| キー | 動作 |
|---|---|
| `1` / `2` / `3` | 体数 8 / 12 / 16 |
| `O` | アウトライン ON / OFF（Renderer Feature の有効/無効。ON/OFF 差分が輪郭の純コスト） |
| `F` | 雨天 ON / OFF（`OutlineWeather.RainAmount`） |
| `P` | 計測開始/停止（一定フレーム数を平均） |
| `C` | 計測結果を CSV として `Application.persistentDataPath` に書き出し＋Console に出力 |

### 計測の進め方（推奨）

1. シーンを Play し、体数を `3`（16体）にする
2. `O` で Outline ON、`F` で晴天（Rain OFF）を確認して `P` → 完了ログを控える
3. `F` で雨天 ON にして `P`
4. `O` で Outline OFF にして、晴天 / 雨天それぞれ `P`（ベースライン）
5. 必要なら体数 8 / 12 でも同様に取る
6. 最後に `C` で CSV を書き出す
7. 体数軸の自動スイープは `OutlinePerfHud.RunAxisSweep()`（12条件×2巡）

### 暗色検証枠

計測シーンは先頭1体を `#44` 黒客相当 `(0.04, 0.04, 0.06)` の輪郭色にしている（`OutlinePerfDirector.includeDarkTestCustomer`）。
マスク A チャンネルの存在フラグ方式で、暗い色でも他色と同じ太さのリングが出ることを確認する枠。残してある。

---

## 1. 計測結果

単位: fps は平均 / 1% low、時間は ms。空欄は実測で埋める。

### 16体

| 条件 | Outline | 天候 | avg fps | 1% low | CPU ms | GPU ms | DrawCalls | SetPass | Batches |
|---|---|---|---|---|---|---|---|---|---|
| 16体 | ON | 晴天 | | | | | | | |
| 16体 | ON | 雨天 | | | | | | | |
| 16体 | OFF | 晴天 | | | | | | | |
| 16体 | OFF | 雨天 | | | | | | | |

### 12体

| 条件 | Outline | 天候 | avg fps | 1% low | CPU ms | GPU ms | DrawCalls | SetPass | Batches |
|---|---|---|---|---|---|---|---|---|---|
| 12体 | ON | 晴天 | | | | | | | |
| 12体 | ON | 雨天 | | | | | | | |
| 12体 | OFF | 晴天 | | | | | | | |
| 12体 | OFF | 雨天 | | | | | | | |

### 8体

| 条件 | Outline | 天候 | avg fps | 1% low | CPU ms | GPU ms | DrawCalls | SetPass | Batches |
|---|---|---|---|---|---|---|---|---|---|
| 8体 | ON | 晴天 | | | | | | | |
| 8体 | ON | 雨天 | | | | | | | |
| 8体 | OFF | 晴天 | | | | | | | |
| 8体 | OFF | 雨天 | | | | | | | |

### 純コスト（Outline ON − OFF）

| 体数 | 天候 | Δ avg fps | Δ CPU ms | Δ GPU ms | Δ DrawCalls | メモ |
|---|---|---|---|---|---|---|
| 16 | 晴天 | | | | | |
| 16 | 雨天 | | | | | |

---

## 2. 機能確認（負荷以外）

| 項目 | 結果 | メモ |
|---|---|---|
| 遮蔽物（鳥居・提灯）の裏で輪郭が透けない | □ OK　□ NG | ステンシルパスの深度テスト + 色マスクの手動深度比較 |
| お守り5色が客ごとに切り替わる | □ OK　□ NG | |
| 暗色（黒客相当）でも同じ太さの輪郭が出る | □ OK　□ NG | 計測シーン先頭1体 |
| 雨天で発光が弱まる | □ OK　□ NG | 仮パラメータ。負荷はほぼ変わらない想定 |
| 太さが画面ピクセル固定に見える | □ OK　□ NG | #44 ScreenConstant 相当。低解像度時は `thicknessPx * scale` 換算 |
| `maskResolutionScale` 1.0 / 0.5 / 0.25 で描画できる | □ OK　□ NG | 見かけ太さは揃う。0.25 はジャギー増 |

---

## 3. 結論（60fps 暫定目標）

- [ ] **OK** — 16体・晴天/雨天とも 60fps を満たす
- [ ] **条件付き OK** — 満たす条件: ____________（例: 解像度 ____、品質 ____）
- [ ] **NG** — 満たさない。下記の軽量化を適用する

理由・根拠:

```
（ここに記入）
```

### Editor 上の体数軸スイープ所見（実装確認時・参考）

`OutlinePerfHud.RunAxisSweep(25)` を 12条件×2巡で実行（Editor / ノートPC実機ではない）。

| 体数 | Outline ON / 晴天 GPU ms（2巡平均） |
|---|---|
| 8 | ≈ 1.97 |
| 12 | ≈ 1.76 |
| 16 | ≈ 1.72 |

- **判定: 8体と16体で GPU ms に有意な増加は見えない**（むしろノイズ範囲で 16 の方がわずかに低い）。
- DrawCalls は体数に比例して増える（ON 時 約 88→110→133）が、GPU 時間はほぼ変わらない。
- **結論: Compose のフルスクリーン `(2r+1)²` ダイレートが支配的で、体数を減らしても軽くならない。**
  軽量化は体数間引きより、ダイレート早期棄却・マスク解像度・半径低減が効く。
- **雨天差:** 16体 Outline ON で晴天/雨天の GPU・fps に再現性のある大きな差は出なかった（Compose の `lerp` 1 個程度では説明できる差しか無い）。
  以前の Editor 報告（晴天≈125 / 雨天≈104）はヒッチや計測順のドリフトが疑わしい。実機では再確認すること。

---

## 4. 60fps を満たさなかった場合の軽量化選択肢

効果の目安は相対的。実機で ON/OFF 差分を取り直してから採用すること。

| 手段 | 期待効果 | トレードオフ | 推奨度（仮） |
|---|---|---|---|
| **ダイレートの早期棄却** | マスク近傍が無いピクセルを即 `discard`（粗いミップやタイル単位判定でさらに削れる）。輪郭は画面のごく一部なので効きが大きい | 実装コスト。端の取りこぼしに注意 | **高（まずここ）** |
| マスクRT解像度を 1/2 | GPU・帯域ダウン大。Compose のダイレートも軽くなる。**ステンシル/色マスク分離後は実際に使える**（`maskResolutionScale=0.5`） | 縁がジャギーになる。細い輪郭で目立つ | 高 |
| マスクRT解像度を 1/4 | さらに軽い（`maskResolutionScale=0.25` も動作確認済み）。ただし `radius = max(1, round(thicknessPx * scale))` の下限により、`thicknessPx=3` では実効太さが画面 4px に張り付く（`thicknessPx < 4` を細く表現できない） | 品質低下が大きい。遠帯では潰れる可能性。細さを保つなら 1/4 は不向き | 中（1/2 で足りないとき） |
| ダイレート半径を下げる | Compose のサンプル数が二乗で減る | 輪郭が細くなり #44 の視認性を損なう | 高 |
| ダイレートを2パス分離（横→縦） | 半径 R で O(R²)→O(R) | 実装コスト・中間RTが1枚増える | 中（半径を保てないとき） |
| 遠帯の客の輪郭を細くする / 間引く | DrawCalls は減るが、上記のとおり GPU 支配項はダイレート側 | 遠方の識別性が落ちる（#44 の論点と衝突）。体数軸スイープでも GPU 差は小さかった | 低 |
| SSAO Renderer Feature を切る | ベースライン自体が軽くなる | 画面の立体感が減る。Outline 純コストとは別枠 | 高（余力確保） |
| レンダースケール / 解像度を下げる | 全体の GPU 負荷が下がる | UI・輪郭とも解像感が落ちる | 中 |
| 影の解像度・カスケード段数を下げる | 影パスが軽くなる | 影のにじみ。Outline とは独立 | 中 |

### 推奨の当たり順（仮）

1. **ダイレートの早期棄却** と **マスクRT 1/2**（見た目への影響が限定的で効きやすい。1/2 は実装済みで切替可能）
2. それでも足りなければ **ダイレート半径の見直し** / **SSAO OFF** や影品質ダウンでベースラインを稼ぐ
3. それでもダメなら **2パスダイレート** を検討（遠帯間引きは GPU 効果が薄い可能性が高い）

---

## 5. 実装メモ（参照用）

```
Assets/Members/Jungaya/Scripts/Outline/
  OutlineTarget.cs              対象の自己登録 + 色/パターン
  OutlineRendererFeature.cs     Stencil + Mask + Compose（RenderGraph・3パス）
  OutlineWeather.cs             雨天・距離減衰の仮パラメータ
  OutlinePattern.cs             Solid/Dashed/Wavy（拡張点。A は +1 エンコード）
  OutlinePerfDirector.cs        計測用 16体配置（暗色検証枠あり）
  OutlinePerfHud.cs             ProfilerRecorder / FrameTimingManager / RunAxisSweep
  Shaders/OutlineStencil.shader 内側ステンシルのみ（フル解像度）
  Shaders/OutlineMask.shader    色マスク（任意解像度・手動深度比較）
  Shaders/OutlineCompose.shader ダイレート合成

Assets/Members/Jungaya/Editor/
  OutlinePerfSceneBuilder.cs    シーン生成メニュー

Assets/Members/Jungaya/Perf/    ← Builder が生成する成果物
Assets/Members/Jungaya/Scenes/OutlinePerf.unity
```

**Mock/ は変更していない**（#44 の検証資産のまま）。本番差し替え点は引き続き `MockCustomerOutline` のコメントどおり、この Outline 一式へ繋ぐ想定。

### 遮蔽の仕組み（要約）

- **Stencil パス:** フル解像度のカメラ depth-stencil に `ZTest LEqual` + `Stencil Replace`（ColorMask 0）
- **Mask パス:** 任意解像度のマスクRTのみ。深度アタッチメント無し。`_CameraDepthTexture` を NDC 由来のスクリーンUVでサンプルし、奥なら `discard`
- **Compose:** `Stencil NotEqual` で内側を除外し、マスクのダイレート外周だけを合成

### パターン拡張点

Compose シェーダの `ApplyPattern` に `// TODO(#色覚対応): Dashed / Wavy をここに実装` を残してある。現状は Solid のみ。
マスク A は `(1 + patternId) / 255`（0=無し）。
